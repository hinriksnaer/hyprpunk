# Desktop Mode Hyprland Overrides
# Sourced last in hyprland.conf - overrides all defaults

# Ultrawide monitor configuration
monitor = HDMI-A-1, 7680x2160@120, 0x0, 1.5

# Master layout optimized for ultrawide displays
general {
    layout = master
}
