module:
  name: neovim-custom
  description: Custom Neovim configuration with advanced LSP, DAP, and plugin setup
  dependencies:
    - cli-tools
  priority: 7

lifecycle:
  before:
    - install
  after: []

packages:
  dnf:
    - ripgrep
    - fzf
  cargo: 
    - tree-sitter-cli
  npm: []

stow:
  target: $HOME
  conflicts: warn
