module:
  name: bluetui
  description: Bluetooth TUI manager
  dependencies:
    - rust

lifecycle:
  before: []
  after:
    - setup-bluetui

packages:
  dnf:
    - dbus-devel
  cargo:
    - bluetui
  npm: []

stow:
  target: $HOME
  conflicts: warn
