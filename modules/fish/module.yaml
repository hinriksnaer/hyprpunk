module:
  name: fish
  description: Fish shell setup for Fedpunk - includes Starship, Fisher, and base configuration
  dependencies:
    - rust
  priority: 5

lifecycle:
  before: []
  after:
    - install
    - setup-fisher

packages:
  copr:
    - atim/starship
  dnf:
    - fish
    - util-linux-user
    - starship
    - procps-ng
  cargo: []
  npm: []

stow:
  target: $HOME
  conflicts: warn
